'use strict';

function gistify(url) {
    // Parse url to determine whether the url is valid and needs to be converted as a github link or gist link.

    // Convert as a github link

    // Convert as a gist link
    
    return url;
}

export default gistify;
